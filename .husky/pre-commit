#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run TypeScript build to catch compilation errors
echo "🔍 Validando TypeScript..."
cd frontend && npm run build

if [ $? -ne 0 ]; then
  echo "❌ Build falló. No se puede hacer commit con errores de TypeScript."
  exit 1
fi

echo "✅ Build exitoso!"
